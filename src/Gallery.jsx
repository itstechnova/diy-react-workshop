import React, { useState } from "react";

/*
  pictures is our array of picture objects, each object with a
    1. "hot" pic source that is shown normally
    2. "goof" pic source that only shows on hover
    3. short text description that is shown at the top
*/
const pictures = [];


/*
  PictureCard is an image that changes on hover. 
*/
const PictureCard = () => {
  return (
    <>
      {/* Something */}
    </>
  );
};


/*
  Gallery is an the component that we use on TechNova's Meet the Team section 
  (https://www.itstechnova.org/#contact) where the description at the top 
  changes depending on which image was hovered on most recently. 
  It renders the PictureCards.
*/
export const Gallery = () => {
  return (
    <>
      <h1>Put your code into Gallery.jsx!</h1>
      {/* Something */}
    </>
  );
};

export default Gallery;
